

class System:
    def __init__(self, name, inputs=dict(), outputs=dict(), process=None):
        self.name = name
        self.inputs = inputs
        self.outputs = outputs
        self.process = process
    
    def convertMass():
        pass
        # Placeholder for mass conversion logic
